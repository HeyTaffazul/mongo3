<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit chats</title>
</head>
<body>
    <h3>Edit your chat: <%=  chat._id %> </h3>
    <p>This chat was send from <%= chat.from %> to <%= chat.to%></p>

    <Form method="POST" action="/chats/<%= chats._id%>?_method=PUT">
        <textarea name="msg" cols="10" rows="15"><%= chat.msg %></textarea><br/>
        <button>Edit</button>
    </Form>

</body>
</html>